# ================================
# Stage 1: Build PHP Application
# ================================
FROM php:8.2-fpm-bullseye AS vendor

# Set working directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    git \
    unzip \
    libpng-dev \
    libjpeg-dev \
    libfreetype6-dev \
    libonig-dev \
    libxml2-dev \
    zip \
    curl \
    libicu-dev \
    libpq-dev \
    supervisor \
    nginx \
    && docker-php-ext-configure gd --with-freetype --with-jpeg \
    && docker-php-ext-install -j$(nproc) gd mbstring pdo pdo_pgsql xml bcmath intl opcache \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

# Copy application code
COPY backend/ ./

# Ensure directories exist
RUN mkdir -p database/seeders database/factories

# Install Composer globally
COPY --from=composer:2 /usr/bin/composer /usr/bin/composer

# ================================
# Composer Install with robust error handling
# ================================
RUN set -eux; \
    echo "Running composer install..."; \
    if ! composer install --no-dev --prefer-dist --no-interaction --optimize-autoloader; then \
        echo ""; \
        echo "======================================================"; \
        echo "ERROR: composer.lock is out of date with composer.json!"; \
        echo "Missing packages detected. Recommended solution:"; \
        echo "  1) Run 'composer update' locally and commit composer.lock"; \
        echo "  2) Then rebuild the Docker image"; \
        echo "======================================================"; \
        echo ""; \
        # Optional: auto-fix for development builds (remove in production) \
        composer update --no-dev --prefer-dist --no-interaction --optimize-autoloader || exit 1; \
    fi

# ================================
# Stage 2: Production Image
# ================================
FROM php:8.2-fpm-bullseye

WORKDIR /app

# Copy built vendor files from previous stage
COPY --from=vendor /app /app

# Copy Nginx and supervisor configuration if needed
COPY docker/nginx.conf /etc/nginx/nginx.conf
COPY docker/supervisord.conf /etc/supervisor/conf.d/supervisord.conf

# Expose port for FPM
EXPOSE 9000

# Start supervisord to run PHP-FPM + Nginx
CMD ["/usr/bin/supervisord", "-n"]
