name: Latest-main deployment guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    name: Guard deployment revisions
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment policy
        run: |
          echo 'Self-hosted GitOps deployment must reconcile the newest main revision.'
          echo 'The existing deployment workflow remains the authoritative deployment path.'
