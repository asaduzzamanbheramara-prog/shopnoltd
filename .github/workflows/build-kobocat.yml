name: build shopnoltd-kobocat
on:
  push:
    branches: [main]
    paths: ['kobocat/**', '.github/workflows/build-kobocat.yml']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v5
        with:
          context: ./kobocat
          push: true
          tags: |
            ghcr.io/asaduzzamanbheramara-prog/shopnoltd-kobocat:main
            ghcr.io/asaduzzamanbheramara-prog/shopnoltd-kobocat:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
