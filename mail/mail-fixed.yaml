apiVersion: apps/v1
kind: Deployment
metadata:
  name: mail-server
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mail-server
  template:
    metadata:
      labels:
        app: mail-server
    spec:
      containers:
      - name: mail-server
        image: tvial/docker-mailserver:latest
        ports:
        - containerPort: 25
        env:
        - name: HOSTNAME
          value: "mail.shopnoltd.dpdns.org"
        - name: DOMAINNAME
          value: "shopnoltd.dpdns.org"
