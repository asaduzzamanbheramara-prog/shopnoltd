apiVersion: v1
kind: Service
metadata:
  name: foundation-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: foundation-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: foundation-service
