apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: android-portal
  namespace: shopno-platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - shopnoltdandroid.shopnoltd.dpdns.org
        - shopnoltd.dpdns.org
      secretName: shopno-wildcard-tls
  rules:
    - host: shopnoltdandroid.shopnoltd.dpdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: android-portal
                port:
                  number: 80
    - host: shopnoltd.dpdns.org
      http:
        paths:
          - path: /downloads
            pathType: Prefix
            backend:
              service:
                name: android-portal
                port:
                  number: 80
