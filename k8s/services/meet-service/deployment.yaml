apiVersion: apps/v1
kind: Deployment
metadata:
  name: meet-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: meet-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: meet-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: meet-service
          image: ghcr.io/asaduzzamanbheramara-prog/shopnoltd/meet-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: meet-service-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: meet-service-secret
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: meet-service-secret
                  key: REDIS_URL
            - name: JITSI_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: meet-service-secret
                  key: JITSI_JWT_SECRET
          resources:
            requests:
              cpu: 20m
              memory: 96Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
