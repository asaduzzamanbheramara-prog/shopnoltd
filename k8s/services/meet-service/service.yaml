apiVersion: v1
kind: Service
metadata:
  name: meet-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: meet-service
  ports:
    - name: http
      port: 80
      targetPort: http
