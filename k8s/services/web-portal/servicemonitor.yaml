apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: web-portal
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: web-portal
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: web-portal
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
