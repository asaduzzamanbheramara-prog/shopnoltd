apiVersion: v1
kind: Service
metadata:
  name: tenant-router
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: tenant-router
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: tenant-router
