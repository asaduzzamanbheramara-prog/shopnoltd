apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tenant-router
  namespace: shopno-platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.tls: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tenant-router.shopnoltd.dpdns.org
      secretName: tenant-router-tls
  rules:
    - host: tenant-router.shopnoltd.dpdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tenant-router
                port:
                  number: 80
