apiVersion: v1
kind: Secret
metadata:
  name: live-service-secret
stringData:
  # Provision real values through the cluster secret manager. Never commit them.
  DATABASE_URL: postgresql+asyncpg://shopno:REPLACE_ME@postgres.shopno-data.svc.cluster.local:5432/live
  REDIS_URL: redis://redis.shopno-data.svc.cluster.local:6379/0
  OWNCAST_API_KEY: REPLACE_ME
