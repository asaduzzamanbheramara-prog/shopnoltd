apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - networkpolicy.yaml

# Runtime secret is provisioned by the cluster secret manager.
# secret.example.yaml is documentation only and is intentionally not rendered.
