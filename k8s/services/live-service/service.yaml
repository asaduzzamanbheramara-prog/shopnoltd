apiVersion: v1
kind: Service
metadata:
  name: live-service
  namespace: shopno-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: live-service
  ports:
    - name: http
      port: 80
      targetPort: http
