apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: domain-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: domain-service
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: domain-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
