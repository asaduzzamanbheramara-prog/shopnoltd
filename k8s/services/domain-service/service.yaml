apiVersion: v1
kind: Service
metadata:
  name: domain-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: domain-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: domain-service
