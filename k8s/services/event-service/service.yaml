apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: event-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: event-service
