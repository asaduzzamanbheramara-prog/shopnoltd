apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: event-service
  namespace: shopno-platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - event.shopnoltd.dpdns.org
      secretName: event-service-tls
  rules:
    - host: event.shopnoltd.dpdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: event-service
                port:
                  number: 80
