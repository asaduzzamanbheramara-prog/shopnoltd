apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: opensearch
  namespace: shopno-data
  labels:
    app.kubernetes.io/name: opensearch
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: opensearch
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
