apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: shopno-payments
  labels:
    app.kubernetes.io/name: payment-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: payment-service
