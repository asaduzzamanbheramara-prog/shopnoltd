apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-platform
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: ai-platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ai-platform
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
