apiVersion: v1
kind: Service
metadata:
  name: chatwoot
  namespace: shopno-apps
  labels:
    app.kubernetes.io/name: chatwoot
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: chatwoot
