apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: worker-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: worker-service
