apiVersion: v1
kind: Service
metadata:
  name: kf
  namespace: toolbox
  labels:
    app.kubernetes.io/name: kf
    app.kubernetes.io/part-of: shopnoltd
spec:
  type: ClusterIP
  selector:
    app: kf
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
