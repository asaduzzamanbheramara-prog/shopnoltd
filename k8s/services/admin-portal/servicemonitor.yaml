apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: admin-portal
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: admin-portal
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: admin-portal
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
