apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jitsi-prosody
  namespace: shopno-apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.tls: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - jitsi-prosody.shopnoltd.dpdns.org
      secretName: jitsi-prosody-tls
  rules:
    - host: jitsi-prosody.shopnoltd.dpdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jitsi-prosody
                port:
                  number: 80
