apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: code-server
  namespace: shopno-apps
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: code-server
