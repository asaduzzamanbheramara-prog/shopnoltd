apiVersion: v1
kind: Service
metadata:
  name: pc-portal
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: pc-portal
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: pc-portal
