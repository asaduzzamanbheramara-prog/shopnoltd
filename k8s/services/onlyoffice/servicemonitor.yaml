apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: onlyoffice
  namespace: shopno-apps
  labels:
    app.kubernetes.io/name: onlyoffice
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: onlyoffice
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
