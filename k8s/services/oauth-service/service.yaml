apiVersion: v1
kind: Service
metadata:
  name: oauth-service
  namespace: shopno-identity
  labels:
    app.kubernetes.io/name: oauth-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: oauth-service
