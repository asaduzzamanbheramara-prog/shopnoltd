apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-service
  namespace: shopno-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: messaging-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: messaging-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: messaging-service
          image: ghcr.io/asaduzzamanbheramara-prog/shopnoltd/messaging-service:d7d17e6703ef112f1defd6f9a13e7be5ef7428fa
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: messaging-service-secret
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: messaging-service-secret
                  key: REDIS_URL
          envFrom:
            - configMapRef:
                name: messaging-service-config
          resources:
            requests:
              cpu: 20m
              memory: 96Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
