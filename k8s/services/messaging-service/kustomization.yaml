apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: shopno-platform

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - networkpolicy.yaml

# secret.example.yaml is documentation only; runtime credentials are provisioned
# by the cluster secret manager.
