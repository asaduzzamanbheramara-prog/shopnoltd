apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: shopno-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: messaging-service
  ports:
    - name: http
      port: 80
      targetPort: http
