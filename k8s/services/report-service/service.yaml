apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: report-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: report-service
