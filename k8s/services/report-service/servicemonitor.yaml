apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: report-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: report-service
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: report-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
