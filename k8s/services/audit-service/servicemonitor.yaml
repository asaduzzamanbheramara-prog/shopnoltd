apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: audit-service
  namespace: shopno-platform
  labels:
    app.kubernetes.io/name: audit-service
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: audit-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
