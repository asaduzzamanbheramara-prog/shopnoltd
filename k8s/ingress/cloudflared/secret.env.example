# Cloudflare Tunnel credential environment file template.
# Keep the real file local/secret-managed; NEVER commit the real credential.
# The value must be the complete JSON credential for the existing tunnel.
CREDENTIALS_JSON='REPLACE_WITH_REAL_CLOUDFLARE_TUNNEL_CREDENTIALS_JSON'
